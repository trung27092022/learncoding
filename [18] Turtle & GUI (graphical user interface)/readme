# the turtle
# GUI (Graphical User Interface) (before was "Text Interface")

# Always remember: to be effective, these 2 below lines must be put below (after) performing the main needed function (method):
screen = Screen()
screen.exitonclick()
